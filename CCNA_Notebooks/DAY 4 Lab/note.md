enable
exit
conf t  

hostname [R1]     // in config mode
enable password [CCNA]     //in config mode
show running-config         //in privileged exec mode
service password-encryption   // in config mode

do show running-config      // in config mode
enable secret [Cisco]        //in config mode

write 
write memory
copy running-config startup-config

